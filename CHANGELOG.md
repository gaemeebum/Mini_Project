# Changelog: Meeting Room Booking System

**โปรเจกต์:** ระบบจองห้องประชุม (Meeting Room Booking System)

---

## [v1.0.0] - 2026-08-04

### Added
* **LO1:** กำหนดโครงสร้างข้อมูลคงที่ `ROOMS` ด้วย `tuple` เพื่อเก็บรายชื่อห้องประชุมป้องกันการแก้ไข
* **LO2:** สร้างโครงสร้างข้อมูลรายการจอง `bookings` แบบ `list of dict` เพื่อให้รองรับการเพิ่ม/แก้ไข/ลบข้อมูลได้ยืดหยุ่น
* **LO2:** เพิ่มฟังก์ชัน `search_bookings` เพื่อค้นหารายการจองตามชื่อห้องหรือวันที่ด้วย List Comprehension
* **LO3:** สร้างระบบเมนูโต้ตอบหลัก `main()` ด้วย `while loop` ร่วมกับ `if/elif/else` รองรับตัวเลือกเมนู 1–5
* **LO4:** ระบบ Defensive Programming ตรวจสอบห้องประชุมที่มีอยู่จริง ตรวจสอบช่องข้อมูลว่าง และป้องกันการจองซ้ำในวัน/เวลาเดียวกัน ในฟังก์ชัน `book_room`
* **LO4:** ระบบ Defensive Programming ใน `cancel_booking` ตรวจสอบความถูกต้องของอินพุตตัวเลขและขอบเขตดรรชนี (Index) ก่อนลบข้อมูล เพื่อป้องกัน `IndexError` และ `ValueError`
## [v0.4.0] - 2026-07-XX
